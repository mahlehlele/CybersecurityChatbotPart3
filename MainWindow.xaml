<Window x:Class="CybersecurityChatbot.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cybersecurity Awareness Chatbot POE"
        Height="820"
        Width="1200"
        MinHeight="720"
        MinWidth="1050"
        WindowStartupLocation="CenterScreen"
        Background="#EEF6F7">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="4" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="4" />
            <Setter Property="Padding" Value="8" />
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#0F3443" CornerRadius="12" Padding="14" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Image Grid.Column="0" Source="Assets/cybersecurity-logo.png" Width="76" Height="76" Margin="0,0,16,0" Stretch="Uniform" />
                <StackPanel Grid.Column="1">
                    <TextBlock Text="Cybersecurity Awareness Chatbot - Part 3 POE" Foreground="White" FontSize="26" FontWeight="Bold" />
                    <TextBlock Text="Part 2 chatbot + JSON task assistant + quiz + NLP simulation + activity log" Foreground="#BDE8E5" FontSize="14" />
                    <TextBlock x:Name="AsciiArtTextBlock" Foreground="#D8F3DC" FontFamily="Consolas" FontSize="11" Margin="0,6,0,0" />
                </StackPanel>
                <StackPanel Grid.Column="2" VerticalAlignment="Center">
                    <Button Content="Replay voice" Click="ReplayVoiceButton_Click" Background="#BDE8E5" />
                    <TextBlock x:Name="StatusTextBlock" Foreground="White" Text="Ready" TextAlignment="Right" Margin="4" />
                </StackPanel>
            </Grid>
        </Border>

        <TabControl x:Name="MainTabs" Grid.Row="1">
            <TabItem x:Name="ChatTab" Header="Chat">
                <Grid Margin="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="290" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Border Grid.Column="0" Grid.Row="0" Background="White" CornerRadius="10" Padding="10" BorderBrush="#D0D5DD" BorderThickness="1">
                        <ScrollViewer x:Name="ChatScrollViewer" VerticalScrollBarVisibility="Auto">
                            <StackPanel x:Name="ChatPanel" />
                        </ScrollViewer>
                    </Border>

                    <Border Grid.Column="1" Grid.Row="0" Grid.RowSpan="3" Background="#F8FAFC" CornerRadius="10" Padding="12" Margin="10,0,0,0" BorderBrush="#D0D5DD" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="Session memory" FontSize="18" FontWeight="Bold" Foreground="#0F3443" />
                            <TextBlock x:Name="MemoryTextBlock" Margin="0,8,0,16" FontFamily="Consolas" />
                            <TextBlock Text="Quick demonstrations" FontWeight="Bold" Foreground="#0F3443" />
                            <Button Content="Phishing tip" Tag="Give me a phishing tip" Click="QuickPrompt_Click" />
                            <Button Content="Worried about phishing" Tag="I am worried about phishing" Click="QuickPrompt_Click" />
                            <Button Content="Add 2FA task" Tag="Add a task to enable two-factor authentication" Click="QuickPrompt_Click" />
                            <Button Content="Start quiz" Tag="Start quiz" Click="QuickPrompt_Click" />
                            <Button Content="Show activity log" Tag="Show activity log" Click="QuickPrompt_Click" />
                            <Separator Margin="0,12" />
                            <Button Content="Copy transcript" Click="CopyTranscriptButton_Click" />
                            <Button Content="Clear visible chat" Click="ClearChatButton_Click" />
                        </StackPanel>
                    </Border>

                    <TextBox x:Name="UserInputTextBox" Grid.Column="0" Grid.Row="1" Height="48" FontSize="15" KeyDown="UserInputTextBox_KeyDown" ToolTip="Type your message here and press Enter" />
                    <StackPanel Grid.Column="0" Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Send" Width="110" Click="SendButton_Click" Background="#0F3443" Foreground="White" />
                    </StackPanel>
                </Grid>
            </TabItem>

            <TabItem x:Name="TaskTab" Header="Task Assistant">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Background="#F8FAFC" CornerRadius="10" Padding="12" BorderBrush="#D0D5DD" BorderThickness="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1.1*" />
                                <ColumnDefinition Width="1.5*" />
                                <ColumnDefinition Width="1.1*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Task title" FontWeight="Bold" />
                                <TextBox x:Name="TaskTitleTextBox" ToolTip="Example: Enable two-factor authentication" />
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Description" FontWeight="Bold" />
                                <TextBox x:Name="TaskDescriptionTextBox" ToolTip="Explain what needs to be done" />
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Reminder" FontWeight="Bold" />
                                <TextBox x:Name="TaskReminderTextBox" ToolTip="Example: Remind me in 7 days" />
                            </StackPanel>
                            <StackPanel Grid.Column="3" VerticalAlignment="Bottom">
                                <Button Content="Add Task" Click="AddTaskButton_Click" Background="#0F3443" Foreground="White" />
                                <Button Content="Refresh" Click="RefreshTasksButton_Click" />
                            </StackPanel>
                        </Grid>
                    </Border>

                    <DataGrid x:Name="TasksDataGrid" Grid.Row="1" Margin="0,10" AutoGenerateColumns="False" CanUserAddRows="False" SelectionMode="Single" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" />
                            <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="180" />
                            <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*" />
                            <DataGridTextColumn Header="Reminder" Binding="{Binding Reminder}" Width="190" />
                            <DataGridCheckBoxColumn Header="Complete" Binding="{Binding IsComplete}" Width="90" />
                            <DataGridTextColumn Header="Created" Binding="{Binding CreatedAt}" Width="140" />
                        </DataGrid.Columns>
                    </DataGrid>

                    <Border Grid.Row="2" Background="#F8FAFC" CornerRadius="10" Padding="10" BorderBrush="#D0D5DD" BorderThickness="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0" Content="Mark selected complete" Click="MarkCompleteButton_Click" />
                            <Button Grid.Column="1" Content="Delete selected" Click="DeleteTaskButton_Click" />
                            <TextBlock Grid.Column="2" x:Name="TaskStatusTextBlock" VerticalAlignment="Center" Margin="10,0,0,0" Text="tasks.json is used for storage." />
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>

            <TabItem x:Name="QuizTab" Header="Cyber Quiz">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Horizontal">
                        <Button Content="Start / Restart Quiz" Click="StartQuizButton_Click" Background="#0F3443" Foreground="White" />
                        <TextBlock x:Name="QuizScoreTextBlock" Text="Score: 0 / 15" VerticalAlignment="Center" Margin="12,0" FontWeight="Bold" />
                    </StackPanel>
                    <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="14" Margin="0,10" BorderBrush="#D0D5DD" BorderThickness="1">
                        <TextBlock x:Name="QuizQuestionTextBlock" Text="Click Start / Restart Quiz to begin." FontSize="20" FontWeight="Bold" />
                    </Border>
                    <Border Grid.Row="2" Background="#F8FAFC" CornerRadius="10" Padding="14" BorderBrush="#D0D5DD" BorderThickness="1">
                        <StackPanel>
                            <StackPanel x:Name="QuizOptionsPanel" />
                            <TextBlock x:Name="QuizFeedbackTextBlock" Margin="0,16,0,0" FontSize="15" />
                        </StackPanel>
                    </Border>
                    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button Content="Submit Answer" Click="SubmitQuizAnswerButton_Click" />
                        <Button Content="Next Question" Click="NextQuestionButton_Click" />
                    </StackPanel>
                </Grid>
            </TabItem>

            <TabItem x:Name="ActivityTab" Header="Activity Log">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Horizontal">
                        <Button Content="Show Recent 10" Click="ShowRecentLogButton_Click" />
                        <Button Content="Show More / Full Log" Click="ShowMoreLogButton_Click" />
                        <Button Content="Refresh" Click="RefreshActivityButton_Click" />
                    </StackPanel>
                    <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="14" Margin="0,10,0,0" BorderBrush="#D0D5DD" BorderThickness="1">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <TextBlock x:Name="ActivityLogTextBlock" FontFamily="Consolas" FontSize="14" />
                        </ScrollViewer>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>

        <TextBlock Grid.Row="2" Margin="4,8,4,0" Foreground="#475467" Text="Submit the GitHub link and unlisted YouTube video link on ARC. The ZIP backup must not include bin or obj folders." />
    </Grid>
</Window>
